#include <stdio.h>

int reverse(int number)
{
   int num=0;
   while(number != 0)
   {
       num = num * 10 + number % 10;
       number /= 10;
   }
   return num;
}

int main()
{
    int n;
    scanf("%d", &n);
    printf("%d ", reverse(n));
    return 0;
}
