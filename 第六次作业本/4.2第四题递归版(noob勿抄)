#include <stdio.h>

int fib(int n)
{
   if(n < 3) return 1;
   else return fib(n - 1) + fib(n - 2);
}

int main()
{
    int n, m, i;
    scanf("%d %d", &n, &m);
    for(i=n; i<=m; i++) 
        printf("%d ", fib(i));
    return 0;
}
